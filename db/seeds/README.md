# Seeds

Only synthetic, reviewed test/demo data belongs here. Production sources, candidate data, credentials and licensed payloads must not be committed.

The controlled 1,000-company pilot uses an ignored `private/pilot-registry.json` manifest and the procedure in `docs/operations/pilot-registry.md`. Only aggregate, non-identifying verification evidence may be attached to a release record.
