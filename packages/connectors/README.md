# Connectors package

ATS-specific detection, enumeration, and parsing adapters. Greenhouse, region-aware Lever, and Ashby are enabled.

Connectors are network-free. They emit exact fetch plans for the safe-fetch boundary and parse immutable response artifacts into SDK contracts. Source identity, tenant ownership, completeness, evidence, and versioning rules are documented per connector under `docs/architecture/`.
